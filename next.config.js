/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'st.nettruyenmax.com',
			'st.nettruyenio.com',
			'localhost',
			'novcomic.vercel.app',
		],
	},
};

module.exports = nextConfig;
